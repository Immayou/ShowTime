export enum InputTypes {
  TEXT = 'text',
  SELECT = 'select',
  TEXTAREA = 'textarea',
  PASSWORD = 'password',
  READ_ONLY = 'read_only',
}

export enum UserTypes {
  REGULAR = 'regular',
  ARTIST = 'artist',
}

export enum FielsdOfActivityTypes {
  SINGING = 'singing',
  DANCING = 'dancing',
}
